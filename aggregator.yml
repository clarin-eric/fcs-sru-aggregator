#params
centerRegistryUrl: http://centerregistry-clarin.esc.rzg.mpg.de/restxml/
weblichtUrl: https://weblicht.sfs.uni-tuebingen.de/WebLicht-4/?input=
updateIntervalUnit: HOURS
updateInterval: 6
scanMaxDepth: 3
aggregatorFilePath: /data/fcsAggregator/fcsAggregatorCorpora.json

# use the simple server factory if you only want to run on a single port
server:
  applicationContextPath: /
  type: simple
  connector:
    type: http
    port: 8080

#server:
#  softNofileLimit: 1000
#  hardNofileLimit: 1000
#  applicationConnectors:
#    - type: http
#      port: 8080
#    - type: https
#      port: 8443
#      keyStorePath: example.keystore
#      keyStorePassword: example
#      validateCerts: false
# this requires the npn-boot library on the JVM's boot classpath
#    - type: spdy3
#      port: 8445
#      keyStorePath: example.keystore
#      keyStorePassword: example
#      validateCerts: false
#  adminConnectors:
#    - type: http
#      port: 8081
#    - type: https
#      port: 8444
#      keyStorePath: example.keystore
#      keyStorePassword: example
#      validateCerts: false

# Logging settings.
logging:
  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: WARNING
  # Logger-specific levels.
  loggers:
    # Sets the level for 'com.example.app' to DEBUG.
    eu.clarin.sru.fcs.aggregator: INFO
    eu.clarin.sru.client: INFO

  appenders:
    - type: console
